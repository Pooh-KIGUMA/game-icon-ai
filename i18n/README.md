# Iconia AI i18n

First release languages: Japanese (`ja`), English (`en`), Korean (`ko`), Simplified Chinese (`zh-CN`), Traditional Chinese (`zh-TW`).

The UI should select the browser language automatically, default to Japanese when unsupported, and expose a manual language selector. AI requests should preserve the user's input language while normalizing editing intent internally so image-edit behavior is language-independent.
