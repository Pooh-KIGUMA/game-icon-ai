// Compatibility route for the pricing page configured as /api/billing/checkout.
// The implementation lives in ../checkout.js so both paths use the same checkout handler.
export { default } from '../checkout.js';
